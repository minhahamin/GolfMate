# assets/

정적 이미지 등 번들에 포함되는 리소스. 컴포넌트에서 `import img from '../assets/xxx.png'`로
불러오면 Vite가 빌드 시 해시가 붙은 파일명으로 최적화해준다.

- `golfer-rabbit.png` — GolfMate AI의 골프 캐디 토끼 마스코트. 로그인/회원가입 페이지의
  히어로 이미지, 라운드가 아직 없을 때의 빈 상태 일러스트로 쓰인다.
